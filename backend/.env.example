# Server
PORT=3000
NODE_ENV=development

# PostgreSQL (required)
DATABASE_URL="postgresql://pma_user:pma_password@localhost:5432/project_management?schema=public"

# MongoDB (optional — for cache/analytics)
MONGO_URI="mongodb://localhost:27017/project_management"

# JWT (required)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="1d"

# CORS
CORS_ORIGIN="*"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
